---
- hosts: RH8_3
  tasks:
    - name: Remove java
      yum:
        name: java
        state: absent

    - name: instalar java
      yum: name=java state=latest
    
    - name: Extract tar
      ansible.builtin.unarchive:
        src: git-2.30.0.tar.gz
        dest: /tmp
